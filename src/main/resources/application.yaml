spring:
  application:
    name: aivle_4th_MiniProject_team19

  web:
    resources:
      add-mappings: true

  # ✅ H2를 서버 없이 바로 쓰는 인메모리 방식 (오류 방지 핵심)
  datasource:
    url: jdbc:h2:mem:BOOKSTORE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: always

# 🔥 session.jdbc 제거 (DB 없어서 죽는 원인)
# spring.session.store-type: jdbc ❌ 제거

logging:
  level:
    root: INFO
    org.hibernate.SQL: debug
    org.springframework: INFO
    org.springframework.web: INFO
