version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/app

hooks:
  ApplicationStop:
    - location: deploy-scripts/stop.sh
      timeout: 300
      runas: ec2-user

  ApplicationStart:
    - location: deploy-scripts/start.sh
      timeout: 300
      runas: ec2-user

